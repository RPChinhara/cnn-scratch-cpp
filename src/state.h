#pragma once

enum ThirstState {
    THIRSTY,
    QUENCHED,
    HYDRATED
};

enum HungerState {
    HUNGRY,
    SATISFIED,
    FULL
};

enum EnergyState {
    LOW,
    MEDIUM,
    HIGH
};

enum HealthState {
    HEALTHY,
    INJURED,
    SICK,
    CRITICAL
};

enum class MentalState {
    CALM,
    RELAXED,
    CONTENT,
    FOCUSED,
    ALERT,
    ENGAGED,
    STRESSED,
    ANXIOUS,
    OVERWHELMED,
    PANICKED,
    NEUTRAL
};

enum EmotionState {
    HAPPY,
    SAD,
    ANGRY,
    NEUTRAL
};