#pragma once

enum ThirstState {
    THIRSTY,
    QUENCHED,
    HYDRATED
};

enum HungerState {
    HUNGRY,
    SATISFIED,
    FULL
};

enum EnergyState {
    LOW,
    MEDIUM,
    HIGH
};

enum HealthState {
    HEALTHY,
    INJURED,
    SICK,
    CRITICAL
};

enum MentalState {
    STRESSED,
    HAPPY
};

enum EmotionState {
    HAPPY,
    SAD,
    ANGRY,
    NEUTRAL
};