#pragma once

enum ThirstState {
    THIRSTY,
    QUENCHED,
    HYDRATED
};

enum HungerState {
    HUNGRY,
    SATISFIED,
    FULL
};

enum EnergyState {
    LOW,
    MEDIUM,
    HIGH
};

enum HealthState {
    HEALTHY,
    INJURED,
    SICK,
    CRITICAL
};

enum MentalState {
    LEVEL1,
    LEVEL2,
    LEVEL3,
    LEVEL4,
    LEVEL5,
    LEVEL6,
    LEVEL7,
    LEVEL8,
    LEVEL9,
    LEVEL10,
};

enum EmotionState {
    HAPPY,
    SAD,
    ANGRY,
    NEUTRAL
};